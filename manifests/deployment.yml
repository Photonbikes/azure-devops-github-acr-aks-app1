apiVersion : apps/v1
kind: Deployment
metadata:
  name: new 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: new
  template:
    metadata:
      labels:
        app: new 
    spec:
      containers:
        - name: new 
          image: gurudevopsacr.azurecr.io/new
          ports:

          - containerPort: 80

